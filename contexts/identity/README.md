# Identity Context
Owner: identity lifecycle, authentication identity linkage and identity invariants.
CForex evidence: auth/identity composition.
Dependencies: contracts, security ports. Persistence remains context-owned.
Gate 0: architecture-only.