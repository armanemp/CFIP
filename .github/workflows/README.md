# CI Workflow Contract

CI must evolve into gate-aware verification: formatting/lint/type checks, architecture boundaries, contract compatibility, tests, security scanning, replay/PIT checks and release evidence. Workflow changes must preserve deterministic evidence and avoid bypassing migration gates.