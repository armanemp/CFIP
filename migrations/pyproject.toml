[project]
name = "cfip-database-migrations"
version = "0.1.0"
description = "CFIP canonical PostgreSQL migration runner"
requires-python = ">=3.14,<3.15"
dependencies = [
  "alembic==1.20.0",
  "SQLAlchemy==2.0.52",
  "psycopg[binary]==3.3.5",
]
