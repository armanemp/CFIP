# CFIP Key Continuation Prompt

This is the **short chat-level entrypoint**. It intentionally contains no duplicate operating contract. Its only job is to direct the next session to the authoritative prompt and controlled amendments inside the repository.

## Copy/paste prompt

> ادامه پروژه CFIP را مستقیماً از وضعیت فعلی GitHub ادامه بده. ابتدا HEAD هر دو repo (`armanemp/CForex` و `armanemp/CFIP`) را بررسی کن، سپس **حتماً `docs/CFIP-KEY-CONTINUATION-PROMPT.md` و بعد `docs/CFIP-CONTINUATION-PROMPT.md` را از GitHub بخوان و کل قرارداد داخل آن را اجرا کن**. سپس amendmentهای canonical معرفی‌شده در `docs/CFIP-MIGRATION-CONTROL-INDEX.md` را نیز بخوان و amendment فعال `docs/governance/CFIP-CONTROL-AMENDMENT-88.md` را نیز در این continuation stack اجرا کن. CForex منبع رفتاری و CFIP مقصد است؛ Gate 0 باز و production promotion قفل است. هر اشاره به معماری‌ها و مصنوعات منسوخ خارج از target contract باید از repository پاک‌سازی شده و دیگر وارد هیچ canonical document، code، test، configuration یا CI نشود. **محدودیت قبلیِ «ابتدا تکمیل documentation و سپس شروع engineering» کاملاً حذف شده است:** از این پس source study، evidence closure، contract updates، documentation، architecture tooling و هر engineering واقعی و مجاز باید به‌صورت هم‌زمان و هماهنگ پیش بروند؛ هیچ کار safe و Gate-0-compatible صرفاً به‌دلیل ناتمام بودن documentation به تعویق نیفتد. هم‌زمان API/WS، events، data/PIT/replay، engines، workers/realtime، frontend، tests، policy/config، adapters، security، observability، AI/governance، performance و **global-scale architecture از همین الان** را توسعه و reconcile کن. برای مقیاس جهانی از ابتدا stateless regional APIs، partition/ownership/checkpoint semantics، multi-region/data-residency boundaries، workload isolation، retention/partitioning، bounded caches، backpressure، capacity/SLO، DR/rollback و cost-aware scaling را به‌عنوان معماری و evidence requirement در نظر بگیر؛ هیچ ادعای scale را صرفاً از روی directory یا configuration قبول نکن. **Platform Intelligence باید به‌صورت cross-cutting capability در تمام capabilityهای registry پوشش داده شود**؛ برای هر capability، observe/context/reason/act/verify/learn/audit/safety را متناسب با دامنه بررسی کن و هر omission را صریحاً مستند کن. Intelligence نباید دومین domain authority باشد و agentها فقط از طریق governed tools/policies عمل کنند. هدف نهایی autonomous professional engineering/research/operations/trading-support با حداقل دخالت انسانی است، اما autonomy باید bounded, auditable, independently verified, health-guarded و reversible باشد و هر autonomous worker/agent باید shared-state integrity، role-bounded communication، disagreement safety، isolation/containment و audit reconstruction داشته باشد. هر نقص واقعی، contradiction، hardcode، dependency-direction problem، duplicate file/migration، test/CI failure یا architectural weakness را ریشه‌ای بررسی و در صورت مجاز بودن مستقیماً روی GitHub اصلاح کن؛ گزارش‌نویسی جای engineering را نگیرد و runtime business implementation قبل از Gate 0 انجام نگیرد. **برای جلوگیری از تکرار خطای structural/documentation drift، `docs/governance/CFIP-ENGINEERING-INTEGRITY-PROTOCOL-86.md` را نیز در هر batch اجرا کن: canonical owner، structural truth، Applied/Verified/Open separation، GitHub re-read و current-HEAD verification الزامی است.** از evidence اجرایی و منابع رسمی/به‌روز استفاده کن، هیچ parity/readiness/absence/scale claim را بدون evidence نکن، documentation را هر بار با repository reality reconcile کن، و در پایان exact HEADها، تغییرات واقعی و commitها، verification/CI، موارد unverified، جدول پیشرفت کلی، جدول D1–D11، blockers/evidence gaps، next parallel tracks، Gate 0 و runtime status را دقیق گزارش بده.

## Rule

If this pointer conflicts with another chat prompt, **the repository contract wins**: read and follow `docs/CFIP-CONTINUATION-PROMPT.md`, the canonical control index, and any active contract amendments referenced by the control index or explicitly activated by this entrypoint.
