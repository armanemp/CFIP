[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "cfip-technical"
version = "0.1.0"
description = "Deterministic, dependency-free technical indicator primitives for CFIP"
requires-python = ">=3.14"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
