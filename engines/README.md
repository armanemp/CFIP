# CFIP Analytical Engines

Canonical analytical engine implementations will use one `(engine_id, version)` identity and be reusable by runtime, durable and replay execution planes.

Gate 0 status: contracts/architecture only; implementations remain locked.
